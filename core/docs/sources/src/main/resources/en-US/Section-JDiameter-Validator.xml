<?xml version='1.0'?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [<!ENTITY % BOOK_ENTITIES SYSTEM "User_Guide.ent">%BOOK_ENTITIES;]>

<section id="jdiameter-validator">

	<title>&THIS.PLATFORM; Diameter Stack Validator</title>

	<para>Validator is one of the Stack features. The primary purpose of the Validator is to detect malformed messages, such as an Answer message containing a Destination-Host Attribute Value Pair (<acronym>AVP</acronym>).</para>

	<para>The Validator is capable of validating multi-leveled, grouped AVPs, excluding the following content types:</para>
	<itemizedlist>
		<listitem>
			<para>URI, or Identifier types.</para>
		</listitem>
		<listitem>
			<para>Enumerated types against defined values.</para>
		</listitem>
	</itemizedlist>
	<para>That is, it is capable of checking structural integrity, not content.</para>
	<para>Validator performs the following checks:</para>
	<variablelist>
		<varlistentry>
			<term>Index</term>
			<listitem><para>Checks if <acronym>AVP</acronym>s are in correct place, eg, <literal>Session-Id</literal> must always be encoded before any other <acronym>AVP</acronym>.</para></listitem>
		</varlistentry>
		<varlistentry>
			<term>Multiplicity</term>
			<listitem><para>Checks if message <acronym>AVP</acronym>s occurs proper number of times, eg, if Session-Id is present one and only one time.</para></listitem>
		</varlistentry>
		<!-- TODO: add more checks :) -->
	</variablelist>
	<para><literal>Validator</literal> is called by stack implementation. It is invoked after message is received and before it is dispatched to remote peer. Note that later means that if peer does not exist in local peer table validator is not called, as stack fails before calling it.</para>

	<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
		href="Section-JDiameter-Validator-Configuration.xml" encoding="UTF-8" />
	<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
		href="Section-JDiameter-Validator-Source_Overview.xml" encoding="UTF-8" />

</section>
